import { Check, ShieldCheck } from 'lucide-react'
import { motion } from 'framer-motion'
import type { ReactNode } from 'react'

export function Card({ children, className = '' }: { children: ReactNode; className?: string }) { return <motion.section initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} whileHover={{ y: -2 }} transition={{ duration: .24 }} className={`glass rounded-lg p-5 ${className}`}>{children}</motion.section> }
export function Badge({ children, tone = 'neutral' }: { children: ReactNode; tone?: 'success' | 'danger' | 'neutral' }) { const style = tone === 'success' ? 'bg-mint-400/10 text-mint-400' : tone === 'danger' ? 'bg-coral-400/10 text-coral-400' : 'bg-white/[.07] text-slate-300'; return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-bold ${style}`}>{children}</span> }
export function PasswordStrength({ password = '', compact = false }: { password?: string; compact?: boolean }) { const checks = [password.length >= 8, /[A-Z]/.test(password), /[a-z]/.test(password), /\d/.test(password), /[^A-Za-z0-9]/.test(password)]; const score = checks.filter(Boolean).length; const labels = ['Too weak', 'Weak', 'Fair', 'Good', 'Strong', 'Excellent']; return <div className={compact ? 'space-y-1.5' : 'space-y-2'} aria-live="polite"><div className="flex gap-1">{checks.map((_, index) => <span key={index} className={`h-1.5 flex-1 rounded-full ${index < score ? 'bg-mint-400' : 'bg-white/10'}`} />)}</div><div className="flex justify-between text-xs text-slate-500"><span>{labels[score]}</span><span>{score}/5 requirements</span></div><div className={compact ? 'flex flex-wrap gap-x-4 gap-y-1 text-[11px] text-slate-500' : 'grid grid-cols-2 gap-1 text-xs text-slate-500'}>{['8+ characters', 'Uppercase', 'Lowercase', 'Number', 'Special character'].map((label, index) => <span key={label} className={checks[index] ? 'text-mint-400' : ''}><Check className="mr-1 inline size-3" />{label}</span>)}</div></div> }
export function BrandMark({ compact = false }: { compact?: boolean }) { return <div className="flex items-center gap-2.5"><span className="grid size-9 place-items-center rounded-md bg-mint-400 text-ink-950 shadow-[0_8px_22px_-12px_rgba(69,224,173,.9)]"><ShieldCheck className="size-5" /></span>{!compact && <span className="font-display text-lg font-bold">TradeWise <b className="text-mint-400">AI</b></span>}</div> }
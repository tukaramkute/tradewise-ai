import { Check, ShieldCheck } from 'lucide-react'
import type { ReactNode } from 'react'

export function Card({ children, className = '' }: { children: ReactNode; className?: string }) { return <section className={`glass rounded-lg p-5 ${className}`}>{children}</section> }
export function Badge({ children, tone = 'neutral' }: { children: ReactNode; tone?: 'success' | 'danger' | 'neutral' }) { const style = tone === 'success' ? 'bg-mint-400/10 text-mint-400' : tone === 'danger' ? 'bg-coral-400/10 text-coral-400' : 'bg-white/[.07] text-slate-300'; return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-bold ${style}`}>{children}</span> }
export function PasswordStrength({ password = '' }: { password?: string }) { const checks = [password.length >= 8, /[A-Z]/.test(password), /[a-z]/.test(password), /\d/.test(password), /[^A-Za-z0-9]/.test(password)]; const score = checks.filter(Boolean).length; const labels = ['Too weak', 'Weak', 'Fair', 'Good', 'Strong', 'Excellent']; return <div className="space-y-2" aria-live="polite"><div className="flex gap-1">{checks.map((_, index) => <span key={index} className={`h-1.5 flex-1 rounded-full ${index < score ? 'bg-mint-400' : 'bg-white/10'}`} />)}</div><div className="flex justify-between text-xs text-slate-500"><span>{labels[score]}</span><span>{score}/5 requirements</span></div><div className="grid grid-cols-2 gap-1 text-xs text-slate-500">{['8+ characters', 'Uppercase', 'Lowercase', 'Number', 'Special character'].map((label, index) => <span key={label} className={checks[index] ? 'text-mint-400' : ''}><Check className="mr-1 inline size-3" />{label}</span>)}</div></div> }
export function BrandMark({ compact = false }: { compact?: boolean }) { return <div className="flex items-center gap-2.5"><span className="grid size-9 place-items-center rounded-md bg-mint-400 text-ink-950"><ShieldCheck className="size-5" /></span>{!compact && <span className="font-display text-lg font-bold">TradeWise <b className="text-mint-400">AI</b></span>}</div> }